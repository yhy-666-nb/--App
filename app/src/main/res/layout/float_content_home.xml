<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="8dp">

    <ImageView
        android:id="@+id/iv_target_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@android:drawable/ic_menu_help"
        android:contentDescription="目标应用" />

    <TextView
        android:id="@+id/tv_target_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="未绑定应用"
        android:textSize="14sp"
        android:textColor="@color/jelly_text_secondary"
        android:layout_marginTop="4dp" />

    <Button
        android:id="@+id/btn_read_process"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="读取进程"
        android:textSize="13sp"
        android:textColor="@color/jelly_text_accent"
        android:background="@drawable/jelly_button_bg"
        android:elevation="6dp"
        android:layout_marginTop="12dp"
        android:minWidth="120dp"
        android:minHeight="44dp" />

    <TextView
        android:id="@+id/tv_process_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="状态：未启动"
        android:textSize="12sp"
        android:layout_marginTop="8dp"
        android:textColor="@color/jelly_text_secondary" />
</LinearLayout>
